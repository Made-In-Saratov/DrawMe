export default function bilinear({
  pixels,
  width,
  height,
  newWidth,
  newHeight,
}: {
  pixels: number[]
  width: number
  height: number
  newWidth: number
  newHeight: number
}) {
  return pixels
}
